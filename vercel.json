{
  "installCommand": "cd interface && npm ci",
  "buildCommand": "cd interface && npm run build:web",
  "outputDirectory": "interface/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
